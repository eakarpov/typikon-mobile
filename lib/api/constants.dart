// Домен сайта записан в приложении один раз — здесь. Переезд правится сменой
// siteHost, а не поиском по файлам: до этой правки адрес стоял в пяти местах
// (базовый адрес API, хост подписного календаря, сверка «свой ли хост» в
// client.dart, подпись в fb2-выгрузке и хост ссылок в AndroidManifest), и одно
// из них ломалось тише прочих — см. ownHosts ниже.
//
// siteHost — как домен пишется в тексте, без протокола и www.
// siteHostFull — имя хоста, на который приложение ходит.
const String siteHost = 'typikon.info';
const String siteHostFull = 'www.$siteHost';

// Домены, которые приложение считает своими и которым представляется заголовком
// appHeaderName. Список, а не один siteHost, из-за переезда: typikon.su остаётся
// нашим, пока отвечает (до середины января 2027), — по нему ходят выпущенные
// копии, и заголовок от них должен уходить, иначе сервер прочтёт их молчание как
// «старых клиентов не осталось» и закроет первую версию API. Убирать старый
// домен отсюда — только когда он перестанет отвечать.
const List<String> ownHosts = <String>['typikon.info', 'typikon.su'];

const String apiBaseUrl = 'https://$siteHostFull';
const String dneslovBaseUrl = 'http://dneslov.org';

const Duration apiTimeout = Duration(seconds: 15);

// Приложение представляется серверу этим заголовком: по нему считается, какая доля
// запросов к устаревшей первой версии API идёт от приложения, а какая от прочих
// клиентов. Пока доля прочих велика, v1 закрывать нельзя.
//
// Версию держим здесь строкой и поднимаем вместе с version в pubspec.yaml.
const String appHeaderName = 'X-Typikon-App';
const String appVersion = '2.2.0+9';

// Подписной календарь чтений на сайте. Хост и путь отдельно — из них же
// собирается webcal://-ссылка, которую календари понимают как подписку.
const String calendarFeedHost = siteHostFull;
const String calendarFeedPath = '/calendar.ics';
const String calendarFeedUrl = 'https://$calendarFeedHost$calendarFeedPath';

// dneslov.org — сторонний сайт, и его доступность нам не подконтрольна: он
// периодически принимает соединение, но не отвечает. Ждать общие 15 секунд в
// таком случае значит держать экран пустым всё это время ради дополнения к
// основному содержимому, поэтому терпение здесь заметно короче.
const Duration dneslovTimeout = Duration(seconds: 6);

// Google OAuth web client id: приложение отдаёт его в GoogleSignIn как
// serverClientId, чтобы id_token приходил с этой audience, а бекенд сверяет её
// при проверке токена. Не секрет — публичный идентификатор.
//
// **Это проект времён typikon.su (номер 632612414346), и менять его не надо.**
// Идентификатор OAuth начинается с номера проекта Google Cloud; Firebase, через
// который ходят пуши, живёт в другом — typikon-info, 798720080241. Разведение
// это безобидно: вход в Firebase не заглядывает вовсе. В google-services.json
// `oauth_client` пуст, и serverClientId передаётся отсюда явно, так что
// проектам незачем что-либо знать друг о друге.
//
// ПЕРЕЕХАТЬ НА ПРОЕКТ typikon-info НЕЛЬЗЯ, и это не лень, а устройство Google:
// пара «имя пакета + отпечаток подписи» уникальна во всём Google Cloud, и
// завести такой же Android-клиент во втором проекте не даст — консоль отвечает
// «package name and fingerprint are already in use». Освободить пару можно
// только удалив клиент из старого проекта, то есть у всех уже установленных
// копий разом, до того как они обновятся.
//
// А ГЛАВНОЕ — переезжать незачем. Идентификатор непрозрачен: в нём нет ни
// «su», ни «info», и пользователь его не видит. Видит он имя приложения и
// домен с экрана согласия, а они настраиваются в самом проекте и меняются на
// месте — переименованием, без смены идентификатора и без выпуска.
//
// Поэтому, если на сервере надоело держать две audience, дешевле свести их НА
// ЭТУ: веб правится за минуту, а установленные копии не правятся никогда.
// Обратный ход — перевести приложение на идентификатор веба — стоит выпуска и
// окна, в котором вход сломан у всех, кто ещё не обновился, и всё равно
// требует держать обе audience, пока старые копии не разойдутся.
//
// Список разрешённых клиентов из чужих проектов, который советует справка
// Google, сюда не годится: это возможность Firebase Authentication, а вход
// здесь идёт через свой бекенд.
const String googleServerClientId =
    '632612414346-v35c46qfr6sl39glq93r6g6m6cqkpolq.apps.googleusercontent.com';
