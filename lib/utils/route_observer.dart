import 'package:flutter/material.dart';

/// Наблюдатель за переходами между экранами.
///
/// Нужен странице, которая показывает `MaterialBanner`. Баннер живёт в
/// `ScaffoldMessenger` выше `MaterialApp` и потому не привязан к экрану: снять
/// его при уходе можно, только узнав об уходе. Странице текста хватало `dispose`
/// — она разрушается, когда с неё уходят. Главной не хватает: она корневая,
/// поверх неё кладут другой маршрут, а сама она остаётся жить, и `dispose` не
/// вызывается вовсе. Предложение включить предзагрузку из-за этого висело
/// поверх всех экранов подряд.
///
/// Живёт отдельным файлом, а не в `main.dart`, чтобы страницам не пришлось
/// импортировать `main.dart`, который импортирует их сам.
final RouteObserver<PageRoute<dynamic>> routeObserver = RouteObserver<PageRoute<dynamic>>();
