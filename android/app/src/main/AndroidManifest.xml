<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="su.typikon.typikon"
>
    <!-- Каждое разрешение названо тем, что его просит. Лишние отсюда уже
         вычищались: QUERY_ALL_PACKAGES (его заменяет перечень <queries> ниже, а
         в Google Play он сам по себе повод для отказа), USE_FULL_SCREEN_INTENT и
         FOREGROUND_SERVICE — ни полноэкранных уведомлений, ни службы переднего
         плана в приложении нет. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- background_fetch со stopOnTerminate=false: после перезагрузки задачу
         надо завести снова. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- flutter_local_notifications. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- Чтобы canLaunchUrl увидел календарь, способный подписаться на ленту. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="webcal" />
        </intent>
    </queries>
   <application
        tools:replace="android:label"
        android:label="typikon"
        android:name="${applicationName}"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <meta-data android:name="flutter_deeplinking_enabled" android:value="true" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Ссылки сайта. android:autoVerify означает, что система сама
                 сверит их с /.well-known/assetlinks.json на этих хостах; без
                 сверки Android с двенадцатой версии не отдаёт http-ссылки
                 приложению вовсе — даже выбором из списка.

                 Хосты ровно те, что сайт ставит в ссылки, и ровно с www:
                 отпечаток на голом typikon.info отдаётся редиректом на www, а
                 проверка редиректов не допускает. До Android 11 включительно
                 одна незаверенная строка роняет проверку всех, поэтому голых
                 форм тут нет — они остаются в фильтре ниже.

                 Оба домена, пока старый жив: ссылки с typikon.su розданы и
                 ходят по рукам. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="www.typikon.info" />
                <data android:host="www.typikon.su" />
            </intent-filter>
            <!-- То же без сверки: голые формы домена. Заверить их нельзя, пока
                 сервер отвечает на них редиректом, но отдать приложению их
                 всё же стоит — там, где система спрашивает пользователя.

                 Здесь же остаётся схема app: в заверяемый фильтр её класть
                 нельзя (сверке подлежат только http и https), а ссылок такого
                 вида ни сайт, ни приложение сейчас не делают вовсе. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:scheme="app" />
                <data android:host="typikon.info" />
                <data android:host="typikon.su" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
       <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />
       <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
       <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
           <intent-filter>
               <action android:name="android.intent.action.BOOT_COMPLETED"/>
               <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
               <action android:name="android.intent.action.QUICKBOOT_POWERON" />
               <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
           </intent-filter>
       </receiver>
    </application>
</manifest>
